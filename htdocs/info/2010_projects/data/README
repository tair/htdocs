This directory is in the repository as a placeholder for a directory needed by web server application code.  Actual content of directory is not included as it is dynamic or temporary output from application or it consists of large datafiles that are not stored in the repository

